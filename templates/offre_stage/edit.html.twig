{% extends 'base.html.twig' %}

{% block title %}Edit OffreStage{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Editer l'offre de stage</h1>


        {{ include('offre_stage/_form.html.twig') }}

        <a href="{{ path('offre_stage_index') }}" class="btn btn-primary mt-4" >Retour à la liste</a>

        {{ include('offre_stage/_delete_form.html.twig') }}
    </div>
{% endblock %}
