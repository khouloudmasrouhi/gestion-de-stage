<form method="post" action="{{ path('offre_stage_delete', {'id': offre_stage.id}) }}" style="display: inline-block" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offre_stage.id) }}">
    <button class="btn btn-danger mt-4 ">Supprimer</button>
</form>
