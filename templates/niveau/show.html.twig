{% extends 'base.html.twig' %}

{% block title %}Niveau{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Niveau</h1>

        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ niveau.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ niveau.designation }}</td>
            </tr>
            </tbody>
        </table>

        <a href="{{ path('niveau_index') }}" class="btn btn-primary">Retour à la liste</a>

        <a href="{{ path('niveau_edit', {'id': niveau.id}) }}" class="btn btn-secondary">editer</a>

        {{ include('niveau/_delete_form.html.twig') }}
    </div>
{% endblock %}
