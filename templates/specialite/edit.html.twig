{% extends 'base.html.twig' %}

{% block title %}Editer Specialite{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Editer Specialite</h1>

        {{ include('specialite/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('specialite_index') }}" class="btn btn-primary mt-4">Retour à la liste</a>

        {{ include('specialite/_delete_form.html.twig') }}
    </div>
{% endblock %}
